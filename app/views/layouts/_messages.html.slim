-if not flash
- flash.each do |name, msg|
  - if msg.is_a?(String)
    div class="alert alert-#{name.to_s == 'notice' ? 'success' : 'danger'}"
      button.close[type="button" data-dismiss="alert" aria-hidden="true", style="align:center;"] Close
      = content_tag :div, msg, :id => "flash_#{name}"
